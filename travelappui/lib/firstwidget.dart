import 'package:flutter/material.dart';

class FirstWi extends StatelessWidget {
  const FirstWi({super.key});

  @override
  Widget build(BuildContext context) {
    var Size = MediaQuery.of(context).size;
    return Scaffold(
      body: SingleChildScrollView(
        
      )
    );
  }
}
